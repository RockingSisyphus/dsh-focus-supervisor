# 可选参考资料示例（测试用）

任务：实现 CSV 缺失值检查的最小程序，并能解释设计。

本次必需标准：读入 UTF-8 CSV；区分空字符串与有效的数字零；生成缺失值计数；输入路径不存在时返回明确错误。

允许使用工具：IDE、终端、直接相关的 CSV 文档与 AI 辅助。验收重点是可解释的成果，不要求证明每个字符都由本人敲出。

本资料只是上传/冻结/按需读资料的示例，不是已经执行通过的测试代码；Demo 不会运行此文件。
