# CSV 清洗程序验收演示成果

程序使用 UTF-8 读取 CSV 文件，先验证必需列是否存在，再逐行处理数据。
对于缺失值：空字符串统一视为缺失；数值字段按约定使用零填补，字符串字段保留为空；不静默删除有效行。
清洗后的数据写入新的 CSV 文件，不覆盖原始输入。
应测试空文件、缺失列、正常数据和含空字符串的数据，并比较输出行数与预期结果。

注意：这是演示替身的测试材料，不代表实际代码功能已经被运行验证。
